<?php

/**
 * Implements hook_preprocess_HOOK().
 */
function sharekit_preprocess_page(&$variables) {

  foreach (\Drupal::service('plugin.manager.sharekit')->getDefinitions() as $plugin_id => $plugin) {
    ksm($plugin);
  }

}
